<% unless @user_created_events.empty? %>
  <h1>Created Events</h1>

  <ul>
    <% @user_created_events.each do |event| %>
      <li>
        <%= event.title %><br>
        <%= event.body %><br>
        <%= event.date %><br>
        <%= event.location %>
      </li>
    <% end %>
  </ul>
<% else %>
  <h1>You haven't created any events yet...</h1>
<% end %>
